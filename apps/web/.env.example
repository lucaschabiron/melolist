# Public URL of the API. Must include the scheme (https://... in prod).
# Used by the better-auth client and the Next proxy session check.
NEXT_PUBLIC_API_URL=http://localhost:3000

# Port Next listens on. Railway injects this; optional locally.
# PORT=3001
