{
    "$schema": "https://schema.railpack.com",
    "packages": {
        "bun": "1.3.12",
        "node": "22"
    },
    "steps": {
        "build": {
            "commands": [
                "echo 'No build step for worker service'"
            ]
        }
    },
    "deploy": {
        "startCommand": "cd apps/worker && bun run src/index.ts"
    }
}
