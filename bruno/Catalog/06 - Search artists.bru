meta {
  name: 06 - Search artists
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/catalog/search?q=radiohead
}

headers {
  Cookie: better-auth.session_token={{sessionCookie}}
}

docs {
  Grouped authenticated search. Returns `artists`, `releaseGroups`, and
  per-section fallback metadata.
}
